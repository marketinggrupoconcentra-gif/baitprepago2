{
  "name": "baitprepago2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy",
    "db:migrate": "node db/migrate.js",
    "db:migrate-admin": "node scripts/migrate-admin.js",
    "test:foundation": "node tests/run.js",
    "test:auth": "node tests/admin-auth.js",
    "test": "npm run test:foundation && npm run test:auth"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
